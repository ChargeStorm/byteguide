{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#about","title":"About","text":"<p>This repository is intended to use as a template for creating new python repositories. It contains the basic structure and configuration files required for a python repository with all the latest tools for following best practices.</p>"},{"location":"index.html#features","title":"Features","text":"<p>This section describes the features of the repository and how to use them.</p>"},{"location":"index.html#uv","title":"Uv","text":"<p>Uv is a package manager for python projects which provide lock file support and python version management. It's written in Rust and is faster than other package managers like pipenv, poetry, etc.</p> <p>\ud83d\udce2 NOTE</p> <p>It's usually recommended to pin the lowest python version that the project supports in the <code>.python-version</code> file.</p> <p>Install uv using the following command:</p> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre> <p>Then run the following command to install the dependencies:</p> <pre><code>uv sync\n</code></pre> <p>This will install all package and dev dependencies, as well as the python version specified in the pyproject.toml file to a .venv virtual environment in the root of the project.</p> <p>To add a new dependency, run the following command:</p> <pre><code># To add a dependency\nuv add &lt;package_name&gt;\n\n# To add a dev dependency\nuv add --dev &lt;package_name&gt;\n</code></pre> <p>For more information, visit the uv documentation.</p>"},{"location":"index.html#pre-commit","title":"Pre-commit","text":"<p>Pre-commit is a framework for managing and maintaining multi-language pre-commit hooks. It's written in Python and is widely used in the python community.</p> <p>We choose to use the pre-commit-uv version of pre-commit, which is a fork of the original pre-commit but uses uv as a package manager. Meaning all packages are installed in parallel, significantly reducing the time it takes to install the packages.</p> <p>After running <code>uv sync</code>, pre-commit will be installed in the .venv virtual environment. To install the pre-commit hooks, run the following command:</p> <pre><code>uv pre-commit install\n</code></pre> <p>This will install the pre-commit hooks in the .git/hooks directory, and the hooks will be called on staged files for each commit.</p> <p>To run the pre-commit hooks manually on all files, run the following command:</p> <pre><code>uv pre-commit run --all-files\n</code></pre> <p>For more information, visit the pre-commit documentation.</p>"},{"location":"index.html#testing","title":"Testing","text":"<p>Testing is done using the <code>pytest</code> framework. To run the tests, execute the following command:</p> <pre><code>uv run pytest\n</code></pre> <p>The <code>pyproject.toml</code> file contains the configuration for the <code>pytest</code> framework. This includes coverage configuration which will output code coverage percentage per file for the <code>src/</code> directory in the terminal. It will also generate a HTML report in the <code>htmlcov/</code> directory.</p> <p>To see line by line coverage, open the <code>htmlcov/index.html</code> file in a browser.</p>"},{"location":"index.html#documentation","title":"Documentation","text":"<p>Documentation is built using mkdocs-material. To build the documentation, run the following command:</p> <pre><code>uv run mkdocs build\n</code></pre> <p>This will build the documentation in the <code>site</code> directory. To serve the documentation locally, run the following command:</p> <pre><code>uv run mkdocs serve\n</code></pre> <p>\ud83d\udce2 NOTE</p> <p>The README.md file has been symlinked to the docs/index.md file to be used as the index page for the documentation. This is not a requirement, but it's a conventiant way to keep the README.md file up to date with the documentation.</p> <p>Command to create a symlink: <code>ln -s ../README.md docs/index.md</code>, executed from the root of the project.</p>"},{"location":"index.html#extra-ctek-styling","title":"Extra CTEK styling","text":"<p>Extra CTEK logo and color styling is added to the documentation. The logo resides in the <code>docs/assets</code>. The color styling is added to the <code>docs/stylesheets/extras.css</code> file. Both are included in the <code>mkdocs.yml</code> configuration file.</p>"}]}